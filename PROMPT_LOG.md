# Prompt Log

## Задание 1: Определение агентов и их ролей

### Промпт 1
**Инструмент:** Sisyphus (Gemma 4 31B)
**Промпт:**
```
ЗАдание 1 Ты — системный архитектор в области IoT и мультиагентных систем. Моя предметная область — «Умный дом». Мне необходимо выполнить первое задание лабораторной работы: «Определение агентов и их ролей».

Напиши подробный отчет, структурированный в виде таблицы или списка, для 4 типов агентов:
1. Агент освещения
2. Агент климат-контроля
3. Агент безопасности
4. Агент энергоменеджмента

Для каждого агента ОБЯЗАТЕЛЬНО укажи следующие пункты:
- Роль агента: за что он отвечает в системе.
- Входные данные (Input): какие параметры он принимает в JSON-формате (например, команды от оркестратора или данные с датчиков).
- Выходные данные (Output): что он возвращает в качестве результата (статус выполнения, текущее состояние).
- Бизнес-правила: логика, по которой агент принимает решения (например, «если освещенность < 200 лк, включить свет»).

Стиль текста: технический, академический, подходящий для вставки в пояснительную записку к лабораторной работе. 
```

**Результат:** Сгенерирован подробный технический отчет с описанием четырех типов агентов (Освещение, Климат, Безопасность, Энергоменеджмент). Для каждого агента определены роль, формат входных/выходных JSON-данных и детальные бизнес-правила.
- Создан файл `agents_definition.md` с содержанием технического отчета.

### Итого
- **Количество промптов:** 1
- **Что пришлось исправлять вручную:** Скорректировал структуру JSON-сообщений для обеспечения единообразия между всеми типами агентов и уточнил формулировки бизнес-правил, чтобы они были однозначными для последующей реализации в коде.
- **Время:** ~5 мин

---

## Задание 2: Разработка прототипа агента на Go

### Промпт 1
**Инструмент:** Sisyphus $\to$ golang-pro (Gemma 4 31B)
**Промпт:**
```
Ты — senior Go разработчик с глубоким опытом в построении распределенных систем и работе с брокерами сообщений. Твоя задача — реализовать прототип одного из агентов для мультиагентной системы «Умный дом» на языке Go.

Выбранный агент: Агент освещения (Lighting Agent).

Технические требования:
1. Стек: 
   - Язык: Go (последняя стабильная версия).
   - Библиотека для NATS: github.com/nats-io/nats.go.
   - Формат обмена данными: JSON.
2. Функциональные требования:
   - Подписка: Агент должен подключаться к NATS серверу и подписываться на тему (subject) tasks.lighting.
   - Обработка данных: 
     - Принимать входящее сообщение в формате JSON (структура Task: ID, Type, Payload).
     - Десериализовать Payload в структуру параметров освещения (согласно определению в agents_definition.md).
   - Бизнес-логика: Реализовать простую логику обработки:
     - Если команда set_state и state == "ON", агент должен «включить» свет (имитация через лог).
     - Если в данных датчиков ambient_light < 200, агент должен автоматически сгенерировать команду на включение света, даже если явной команды не было.
   - Ответ: После обработки агент должен опубликовать результат в тему tasks.completed в формате JSON (структура Result: TaskID, Success, Output).
3. Требования к качеству кода:
   - Структура проекта: 
     - main.go — точка входа, инициализация NATS и запуск подписки.
     - internal/agent/ — логика обработки задач и бизнес-правила.
     - internal/models/ — структуры данных для JSON (Task, Result, LightingParams).
   - Логирование: Использовать стандартный пакет log или zap/logrus для вывода событий: «Получена задача ID», «Применение правила: освещенность низкая», «Результат отправлен».
   - Отказоустойчивость: Реализовать корректную обработку ошибок при подключении к NATS и десериализации JSON (чтобы агент не «падал» при получении некорректного сообщения).
   - Graceful Shutdown: Обработка сигналов ОС (SIGINT, SIGTERM) для корректного закрытия соединения с NATS.
4. Ожидаемый результат:
   - Полный исходный код проекта.
   - Файл go.mod с зависимостями.
   - Краткий README.md с инструкцией по запуску и примером JSON-сообщения для тестирования через nats pub.
Результат должен быть промышленного качества: с соблюдением идиоматики Go, четким разделением ответственности и полной готовностью к запуску.
```

**Результат:** Реализован полноценный агент освещения в директории `lighting-agent/`. 
- Создана правильная структура проекта (`internal/agent`, `internal/models`).
- Реализовано подключение к NATS с автоматическим переподключением.
- Внедрена бизнес-логика управления светом и авто-триггер при низком уровне освещенности (< 200 лк).
- Обеспечен корректный Graceful Shutdown через обработку системных сигналов.
- Создан `go.mod` и подробный `README.md` с примерами тестирования.

### Итого
- **Количество промптов:** 1
- **Что пришлось исправлять вручную:** Вручную создал структуру папок проекта (`internal/agent`, `internal/models`), настроил `go.mod` и проверил совместимость версий библиотеки `nats.go` с установленной версией Go.
- **Время:** ~3 мин

---

## Задание 3: Разработка оркестратора на Python

### Промпт 1
**Инструмент:** Sisyphus $\to$ python-pro (Gemma 4 31B)
**Промпт:**
```
ЗАДАЧА: Реализовать профессиональный Оркестратор агентов на Python для мультиагентной системы «Умный дом».

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
Профессиональная реализация оркестратора в директории 'orchestrator/'.
Критерии успеха:
1. Структура проекта:
   - orchestrator/main.py (Точка входа, демонстрация отправки задач)
   - orchestrator/core.py (Класс AgentOrchestrator)
   - orchestrator/models.py (Pydantic-модели для Task и Result)
   - orchestrator/.env.example (Пример переменных окружения)

2. Основной функционал:
   - Использование 'nats-py' и 'asyncio'.
   - Реализация паттерна Request-Response поверх NATS:
     - Оркестратор публикует задачу в конкретную тему (например, 'tasks.lighting').
     - Он слушает тему 'tasks.completed' для получения результатов.
     - Использование внутреннего маппинга (например, Dict[str, asyncio.Future]) для корреляции 'task_id' в ответе с оригинальным запросом.
   - Метод `send_task`:
     - Сигнатура: `async def send_task(self, subject: str, payload: dict, task_type: str, timeout: float = 30.0) -> dict`
     - Должен генерировать уникальный `task_id` (UUID).
     - Должен обрабатывать `asyncio.TimeoutError` и очищать ожидающий Future.
   - Коллбэк `on_result`:
     - Корректный парсинг JSON-ответа и разрешение соответствующего Future.

3. Техническое совершенство:
   - Полная типизация (PEP 484).
   - Надежная обработка ошибок (сбои подключения, ошибки декодирования JSON).
   - Структурированное логирование (использование модуля 'logging').
   - Конфигурация через переменные окружения (NATS_URL).
   - Graceful shutdown (корректное закрытие) соединения NATS.

4. Интеграция:
   - Обеспечить полную совместимость JSON-структуры с Go-агентом освещения:
     - Task: { "id": str, "type": str, "payload": dict/raw_json, "timestamp": str }
     - Result: { "task_id": str, "success": bool, "data": any, "error": str, "timestamp": str }

ТРЕБУЕМЫЕ ИНСТРУМЕНТЫ:
- write (для создания файлов)

ОБЯЗАТЕЛЬНО:
- Использовать Pydantic для валидации данных.
- Гарантировать неблокирующую работу оркестратора.
- Предоставить понятный main.py, который отправляет несколько тестовых задач в тему 'tasks.lighting'.

ЗАПРЕЩЕНО:
- Использовать блокирующие вызовы (например, time.sleep) внутри async-функций.
- Хардкодить NATS URL.

КОНТЕКСТ:
В системе уже есть Go-агент освещения, который слушает 'tasks.lighting' и отвечает в 'tasks.completed'.
```

**Результат:** Реализован полноценный оркестратор в директории `orchestrator/`.
- Внедрена архитектура Request-Response через `asyncio.Future` и NATS.
- Реализована строгая валидация данных с помощью Pydantic.
- Создан `main.py` с четырьмя сценариями тестирования (включая авто-триггер и таймауты).
- Проведена интеграционная проверка: Оркестратор $\to$ Go-Агент $\to$ Оркестратор. Все сценарии пройдены успешно.

### Итого
- **Количество промптов:** 1
- **Что пришлось исправлять вручную:** 
  1. Исправил `AttributeError` в `core.py`, заменив доступ к внутреннему полю `_loop` на `asyncio.get_event_loop()`.
  2. Исправил `UnicodeEncodeError` в `main.py`, заменив символы-эмодзи на текстовые маркеры `[OK]`, `[ERROR]` для совместимости с консолью Windows.
- **Время:** ~15 мин

---

## Задание 4: Настройка коммуникации через NATS

### Промпт 1
**Инструмент:** Sisyphus $\to$ devops-engineer (Gemma 4 31B)
**Промпт:**
```
ЗАДАЧА: Реализовать полноценное Docker-развертывание для мультиагентной системы «Умный дом».

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
Конфигурация Docker, позволяющая запустить NATS-сервер и агентов в контейнерах, обеспечивая при этом возможность взаимодействия с ними из оркестратора, запущенного на хосте.

Критерии успеха:
1. Dockerfile для Lighting Agent:
   - Расположение: `lighting-agent/Dockerfile`
   - Использование multi-stage build:
     - Stage 1 (Build): `golang:1.21-alpine`, загрузка зависимостей, сборка статического бинарного файла (CGO_ENABLED=0 GOOS=linux).
     - Stage 2 (Run): `alpine:latest`, копирование только бинарного файла.
   - Безопасность: использование не-root пользователя.
   - Отсутствие открытых портов (агент является только клиентом NATS).

2. docker-compose.yml:
   - Расположение: корень проекта.
   - Сервисы:
     - `nats`: официальный образ NATS, проброс порта 4222:4222 для доступа с хоста.
     - `lighting-agent`: сборка из `./lighting-agent`, зависимость от `nats`, переменная окружения `NATS_URL=nats://nats:4222`.
   - Сеть: создание выделенной bridge-сети (например, `smarthome-net`).

3. Конфигурация:
   - Обеспечение связи между контейнерами через DNS NATS (service name).
   - Сохранение возможности подключения оркестратора с хоста по адресу `nats://localhost:4222`.

ТРЕБУЕМЫЕ ИНСТРУМЕНТЫ:
- write (для создания Dockerfile и docker-compose.yml)

ОБЯЗАТЕЛЬНО:
- Использовать многоэтапную сборку (multi-stage) для минимизации размера образа агента.
- Создать понятный `docker-compose.yml` с правильными зависимостями.
- Обновить README.md с инструкциями по быстрому запуску через Docker.

КОНТЕКСТ:
Корень проекта: D:\Workspace\MTP_lab13
Путь к агенту: D:\Workspace\MTP_lab13\lighting-agent
Путь к оркестратору: D:\Workspace\MTP_lab13\orchestrator
Оркестратор работает на хосте $\to$ `nats://localhost:4222`.
Агент работает в контейнере $\to$ `nats://nats:4222`.
```

**Результат:** Развернута полноценная инфраструктура в Docker.
- Создан оптимизированный `Dockerfile` для Go-агента (multi-stage, alpine, non-root user).
- Создан `docker-compose.yml`, объединяющий NATS-сервер и Lighting Agent в одну сеть.
- Настроено взаимодействие: Оркестратор (хост) $\to$ NATS (порт 4222) $\to$ Агент (внутри сети Docker).
- Обновлен `README.md` с инструкциями по быстрому запуску через `docker compose up -d`.
- Проведена проверка: все 4 сценария взаимодействия (штатный, авто-триггер, ошибка типа, таймаут) отработали корректно.

### Итого
- **Количество промптов:** 1
- **Что пришлось исправлять вручную:** 
  1. Провел ручное сквозное тестирование системы: запуск NATS через Docker Compose, запуск Go-агента и запуск Python-оркестратора.
  2. Верифицировал логи всех трех компонентов для подтверждения корректности доставки сообщений и работы авто-триггера.
- **Время:** ~25 мин

---

## Задание 5: Логирование и мониторинг

### Промпт 1
**Инструмент:** Sisyphus $\to$ golang-pro, python-pro (Gemma 4 31B)
**Промпт:**
```text
ЗАДАЧА: Реализовать систему расширенного логирования и базового мониторинга для компонентов мультиагентной системы «Умный дом» (Go-агент и Python-оркестратор).

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
Внедрение сквозного логирования с выводом в консоль и файлы, а также реализация счетчиков производительности для отслеживания нагрузки.

ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ:

1. Для Lighting Agent (Go):
   - Логирование:
     - Реализовать двойной вывод логов (Dual Logging) с использованием `io.MultiWriter`.
     - Логи должны одновременно записываться в стандартный вывод (stdout) и в файл `agent.log` (в режиме append).
     - Внедрить явное разделение уровней важности сообщений с помощью префиксов `[INFO]` и `[ERROR]`.
   - Мониторинг:
     - Реализовать потокобезопасный счетчик обработанных задач, используя пакет `sync/atomic`.
     - Значение счетчика должно выводиться в лог при каждой успешной публикации результата в NATS.

2. Для Оркестратора (Python):
   - Логирование:
     - Использовать стандартный модуль `logging`.
     - Настроить два хендлера: `StreamHandler` для вывода в консоль и `FileHandler` для записи в файл `orchestrator.log` (UTF-8, режим append).
     - Установить единый структурированный формат лога: `%(asctime)s [%(levelname)s] %(name)s: %(message)s`.
     - Настроить уровень логирования `INFO` для всех компонентов.
   - Мониторинг:
     - Добавить внутренний счетчик отправленных задач (`_task_count`).
     - Выводить общее количество отправленных задач в лог после каждого вызова метода `send_task`.

3. Общие требования:
   - Логирование не должно блокировать основную работу агентов или оркестратора.
   - Пути к лог-файлам должны быть относительными корня каждого компонента.
   - Код должен соответствовать существующему стилю и быть полностью типизирован.

ВЕРИФИКАЦИЯ:
- Проверить физическое создание файлов `agent.log` и `orchestrator.log` после запуска.
- Убедиться, что сообщения в консоли и файлах идентичны.
- Проверить корректность инкремента счетчиков при прохождении нескольких тестовых сценариев.
```

**Результат:** Реализована система расширенного логирования и мониторинга для всех компонентов.
- **Lighting Agent (Go)**:
  - Настроен `io.MultiWriter` для одновременной записи в консоль и файл `agent.log`.
  - Внедрены уровни `[INFO]` и `[ERROR]`.
  - Добавлен потокобезопасный счетчик обработанных задач (`sync/atomic`), значение которого выводится в логах при каждом ответе агента.
- **Оркестратор (Python)**:
  - Настроен `logging` с двумя хендлерами: `StreamHandler` (консоль) и `FileHandler` (`orchestrator.log`).
  - Установлен единый формат логов: `%(asctime)s [%(levelname)s] %(name)s: %(message)s`.
  - Добавлен счетчик отправленных задач, который выводится в лог после каждого запроса.

**Верификация**: 
- Проверено создание файлов `agent.log` и `orchestrator.log`.
- Подтверждена корректность инкремента счетчиков в логах при прохождении тестовых сценариев.

### Итого
- **Количество промптов:** 1
- **Что пришлось исправлять вручную:** Провел финальную проверку путей к лог-файлам и убедился, что Go-агент пишет логи в правильную директорию при запуске из разных рабочих папок.
- **Время:** ~15 мин

---

## Задание 6: Обработка ошибок и таймаутов

### Промпт 1
**Инструмент:** Sisyphus $\to$ unspecified-high (Gemma 4 31B)
**Промпт:**
```
1. ЗАДАЧА: Реализовать механизм повторных попыток (retry) в методе AgentOrchestrator.send_task в файле orchestrator/core.py.

2. ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
Метод send_task должен пытаться отправить задачу и получить ответ до 3 раз, если возникает TimeoutError или ConnectionError. Если любая из попыток успешна, результат возвращается немедленно. Если все 3 попытки завершились неудачей, выбрасывается последнее исключение.

3. ТРЕБУЕМЫЕ ИНСТРУМЕНТЫ:
- Read
- Edit
- LspDiagnostics

4. ОБЯЗАТЕЛЬНО К ВЫПОЛНЕНИЮ:
- Реализовать цикл повторов (максимум 3 попытки).
- Повторять при: TimeoutError (вызываемый asyncio_wait_for) и ConnectionError (когда nc равен None или произошел сбой подключения к NATS).
- Для каждой попытки повтора:
    - Генерировать НОВЫЙ task_id с помощью uuid4().hex.
    - Создавать новый asyncio.Future и добавлять его в self._pending.
    - Публиковать задачу в NATS.
    - Ожидать ответ, используя существующий помощник asyncio_wait_for.
    - При возникновении ошибки, допускающей повтор, записывать в лог: logger.warning("Retry %d/3 for task %s due to %s", attempt, task_id, error) и ждать 1 секунду перед следующей попыткой.
    - ВСЕГДА удалять неудавшийся task_id из self._pending перед повтором или выходом.
- Сохранить существующее логирование публикации задач и общих счетчиков.
- Обеспечить типобезопасность и следовать существующему стилю кода.

5. ЗАПРЕЩЕНО:
- Изменять сигнатуру метода send_task.
- Удалять существующий функционал.
- Подавлять ошибки типизации.

6. КОНТЕКСТ:
Файл: orchestrator/core.py
Целевой метод: AgentOrchestrator.send_task (строки 122-195)
Текущая реализация просто публикует один раз и ожидает. Необходимо обернуть логику публикации и ожидания в цикл повторов.
```

**Результат:** Реализован отказоустойчивый механизм отправки задач в оркестраторе.
- Внедрен цикл повторных попыток (до 3 раз) при возникновении `TimeoutError` или `ConnectionError`.
- Для каждой попытки генерируется новый уникальный `task_id` для корректной корреляции ответов в NATS.
- Добавлена задержка в 1 секунду между попытками и соответствующее логирование предупреждений.
- Обеспечена очистка маппинга `_pending` при каждой неудачной попытке для предотвращения утечек памяти.

**Верификация**:
- Создан и запущен скрипт `orchestrator/verify_retries.py`, который имитирует отсутствие агента (таймауты) и разрыв соединения.
- Подтверждено, что система делает ровно 3 попытки перед окончательным выбросом исключения.
- Диагностика LSP подтвердила отсутствие ошибок типизации в измененном коде.

### Итого
- **Количество промптов:** 1
- **Что пришлось исправлять вручную:** Не требовалось.
- **Время:** ~10 мин

---

## Задание 7: Запуск нескольких агентов одного типа

### Промпт 1
**Инструмент:** Sisyphus $\to$ unspecified-high (Gemma 4 31B)
**Промпт:**
```text
ЗАДАЧА: Реализовать горизонтальное масштабирование и балансировку нагрузки для агентов освещения.

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
Возможность запуска нескольких экземпляров одного и того же Go-агента, при которой задачи от оркестратора распределяются между ними равномерно (Load Balancing), а не дублируются.

ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ:

1. Изменения в Lighting Agent (Go):
   - Заменить стандартный метод подписки `Subscribe` на `QueueSubscribe`.
   - Использовать имя группы очереди (queue group): "lighting_group".
   - Обновить лог запуска, чтобы было видно, что агент работает в составе группы.

2. Изменения в инфраструктуре (Docker Compose):
   - Модифицировать `docker-compose.yml` для поддержки масштабирования сервиса `lighting-agent`.
   - УДАЛИТЬ параметр `container_name` у сервиса агента (так как фиксированное имя препятствует масштабированию в Docker Compose).
   - Добавить секцию `deploy` с параметром `replicas: 3` для автоматического запуска трех экземпляров.

3. Верификация:
   - Написать тестовый скрипт на Python, который отправляет серию (burst) из 10+ задач в тему 'tasks.lighting'.
   - Проанализировать логи всех запущенных контейнеров агентов, чтобы подтвердить, что разные задачи обрабатывались разными экземплярами агентов.

КОНТЕКСТ:
Файлы для правки: `lighting-agent/internal/agent/lighting.go`, `docker-compose.yml`.
Текущая архитектура: один агент слушает тему, все сообщения приходят всем подписчикам (Fan-out). Нужно перейти к модели очереди (Competing Consumers).
```

**Результат:** Реализована полноценная балансировка нагрузки через NATS Queue Groups.
- **Go-агент**: Переведен на `QueueSubscribe`, что позволило объединить экземпляры в логическую группу `lighting_group`.
- **Docker**: Конфигурация обновлена для поддержки репликации (удален `container_name`, добавлено `deploy.replicas: 3`).
- **Верификация**: С помощью скрипта `orchestrator/verify_lb.py` подтверждено, что задачи распределяются между тремя разными контейнерами (`lighting-agent-1`, `lighting-agent-2`, `lighting-agent-3`).

### Итого
- **Количество промптов:** 1
- **Что пришлось исправлять вручную:** Провел ручную верификацию распределения нагрузки, проанализировав логи нескольких контейнеров.
- **Время:** ~15 мин

---

## Задание 8: Создание API для запуска задач

### Промпт 1
**Инструмент:** Sisyphus $\to$ unspecified-high (Gemma 4 31B)
**Промпт:**
```text
ЗАДАЧА: Создать REST API на базе FastAPI, который служит интерфейсом для отправки задач в мультиагентную систему.

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
Создание файла `orchestrator/api.py` с полноценным веб-сервером, который интегрирован с существующим классом `AgentOrchestrator`.

ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ:

1. Архитектура API:
   - Использовать FastAPI.
   - Реализовать управление жизненным циклом (Lifespan handler):
     - При запуске сервера: создание экземпляра `AgentOrchestrator` и вызов `await orchestrator.connect()`.
     - При остановке сервера: вызов `await orchestrator.close()`.
   - Инстанс оркестратора должен храниться в `app.state`, чтобы быть доступным в эндпоинтах.

2. Эндпоинты:
   - Реализовать метод `POST /tasks`.
   - Тело запроса (JSON) должно валидироваться через Pydantic-модель (`TaskRequest`) и содержать:
     - `subject`: (str) Тема NATS (например, "tasks.lighting").
     - `payload`: (dict) Параметры задачи.
     - `task_type`: (str, optional) Тип операции, по умолчанию "set_state".
     - `timeout`: (float, optional) Время ожидания ответа, по умолчанию 30.0.
   - Ответ эндпоинта: JSON-результат, возвращаемый методом `orchestrator.send_task()`.

3. Обработка ошибок:
   - `TimeoutError` $\to$ HTTP 504 (Gateway Timeout).
   - `ConnectionError` $\to$ HTTP 503 (Service Unavailable).
   - Прочие исключения $\to$ HTTP 500 (Internal Server Error).

4. Качество кода:
   - Полная типизация (Type Hinting).
   - Соблюдение стиля именования и структуры проекта.

КОНТЕКСТ:
- Оркестратор: `orchestrator/core.py`.
- Модели: `orchestrator/models.py`.
- Запуск осуществляется через uvicorn: `uvicorn orchestrator.api:app --reload`.
```

**Результат:** Реализован REST API интерфейс для управления системой.
- Создан файл `orchestrator/api.py` с использованием FastAPI.
- Внедрен механизм `lifespan` для корректного управления соединением с NATS.
- Реализован универсальный эндпоинт `/tasks`, позволяющий управлять любыми агентами системы через HTTP.
- Настроена детальная обработка исключений с возвратом соответствующих HTTP-статусов.

**Верификация**:
- Написан тестовый скрипт `orchestrator/verify_api.py` с использованием `httpx`.
- Проверена цепочка: `HTTP POST /tasks` $\to$ `FastAPI` $\to$ `AgentOrchestrator` $\to$ `NATS` $\to$ `Lighting Agent` $\to$ `Response`.
- Подтвержден статус 200 OK и корректность возвращаемого JSON-ответа.

### Итого
- **Количество промптов:** 1
- **Что пришлось исправлять вручную:** Установил недостающие зависимости `fastapi`, `uvicorn` и `httpx` в виртуальное окружение.
- **Время:** ~20 мин

---

## Задание 9: Тестирование системы

### Промпт 1 (Go-агент)
**Инструмент:** Sisyphus $\to$ unspecified-high (Gemma 4 31B)
**Промпт:**
```text
ЗАДАЧА: Реализовать модульные тесты для Lighting Agent на языке Go.

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
Создание файла `lighting-agent/internal/agent/lighting_test.go` с тестовым покрытием основной бизнес-логики.

ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ:
1. Использовать стандартный пакет `testing`.
2. Тесты для `handleSetState`:
   - Нормальная работа: освещенность > 200, состояние соблюдается.
   - Авто-триггер: освещенность < 200, состояние принудительно "on", яркость корректируется.
   - Некорректный JSON в payload: проверка возврата ошибки.
3. Тесты для `handleMessage`:
   - Валидный тип задачи "set_state".
   - Неизвестный тип задачи.
   - Некорректный формат JSON всего сообщения.
4. Использовать Table-Driven Tests для компактности и расширяемости.
5. Обеспечить отсутствие паник при использовании фиктивного NATS соединения.

КОНТЕКСТ:
Тестируемый файл: `lighting-agent/internal/agent/lighting.go`.
Модели: `lighting-agent/internal/models/models.go`.
```

**Результат:** Создан набор модульных тестов для Go-агента.
- Реализованы тесты для `handleSetState` (включая все сценарии авто-триггера).
- Реализованы тесты для `handleMessage` (валидация типов и JSON).
- Все тесты успешно пройдены (`go test -v`).

### Промпт 2 (Оркестратор)
**Инструмент:** Sisyphus $\to$ unspecified-high (Gemma 4 31B)
**Промпт:**
```text
ЗАДАЧА: Реализовать модульные тесты для AgentOrchestrator на Python.

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
Создание директории `orchestrator/tests/` и файла `test_core.py` с тестами основной логики оркестратора.

ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ:
1. Стек: `pytest` и `unittest.mock` (AsyncMock).
2. Полная изоляция: тесты не должны зависеть от запущенного NATS сервера.
3. Сценарии тестирования:
   - Успешная отправка задачи: имитация публикации в NATS и получение ответа через `_on_result`.
   - Таймаут: имитация таймаута и проверка выброса `TimeoutError`.
   - Механизм Retry: имитация серии сбоев (Timeout/ConnectionError) и проверка того, что оркестратор делает до 3 попыток перед финальным сбоем.
   - Корреляция ответов: проверка, что результат с определенным `task_id` разрешает соответствующий Future, а неизвестные ID игнорируются.
   - Жизненный цикл: проверка вызова методов connect() и close().

КОНТЕКСТ:
Тестируемый файл: `orchestrator/core.py`.
Модели: `orchestrator/models.py`.
```

**Результат:** Реализован тестовый набор для оркестратора.
- Создана структура `orchestrator/tests/`.
- Реализовано 6 тестов, покрывающих критические пути (успех, таймаут, ретраи, корреляция).
- Все тесты успешно пройдены (`pytest`).

### Итого
- **Количество промптов:** 2
- **Что пришлось исправлять вручную:** Настроил установку `pytest` и `pytest-asyncio` в виртуальном окружении, а также переменную `PYTHONPATH` для запуска тестов из корня проекта.
- **Время:** ~40 мин

---

## Задание 10: Документирование архитектуры

### Промпт 1
**Инструмент:** Sisyphus $\to$ writing (Gemma 4 31B)
**Промпт:**
```text
1. ЗАДАЧА: Создать подробный файл архитектурной документации `architecture.md`.
ВАЖНО: НЕ ИЗМЕНЯТЬ `README.md`.

2. ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
Профессиональный Markdown-файл, содержащий обзор системы высокого уровня, диаграмму последовательности Mermaid и детальное описание компонентов.

3. ТРЕБОВАНИЯ К СОДЕРЖАНИЮ:

А. ОБЗОР СИСТЕМЫ:
- Описание цели системы: Распределенное управление устройствами умного дома с использованием архитектуры мультиагентной системы (МАС).
- Объяснение основного потока данных: Внешний запрос -> API-шлюз -> Оркестратор -> Брокер NATS -> Специализированные агенты -> Возврат результата.

Б. ДИАГРАММА ВЗАИМОДЕЙСТВИЯ (Mermaid):
- Создать `sequenceDiagram`, отображающую:
    - Пользователь/Клиент отправляет HTTP POST в FastAPI.
    - FastAPI вызывает метод `send_task` оркестратора.
    - Оркестратор публикует задачу в тему NATS (например, `tasks.lighting`).
    - Одна из реплик Агента освещения (в группе `lighting_group`) получает задачу.
    - Агент обрабатывает бизнес-правила (включая авто-триггер).
    - Агент публикует результат в тему `tasks.completed`.
    - Оркестратор сопоставляет результат по `task_id` и разрешает Future.
    - FastAPI возвращает результат пользователю.

В. ОПИСАНИЕ КОМПОНЕНТОВ:
- FastAPI Gateway: Точка входа, трансляция HTTP в NATS, валидация запросов.
- AgentOrchestrator: Центральный координатор. Реализует паттерн «Запрос-Ответ», маппинг корреляционных ID и логику повторов (до 3 попыток).
- NATS Broker: Высокопроизводительный брокер сообщений. Обеспечивает маршрутизацию по темам и балансировку нагрузки через Queue Groups.
- Lighting Agent (Go): Доменный воркер. Реализует автономные бизнес-правила (Освещенность < 200 лк $\to$ Принудительное ВКЛ).

Г. АРХИТЕКТУРНЫЕ ПАТТЕРНЫ:
- Запрос-Ответ поверх Pub-Sub: Использование UUID и Futures для асинхронной корреляции.
- Конкурирующие потребители (Competing Consumers): Использование NATS Queue Groups для распределения нагрузки между идентичными репликами агентов.
- Отказоустойчивость: Реализация повторных попыток при ошибках соединения или таймаутах.

4. СТИЛЬ:
- Профессиональный, технический и четкий. Использовать заголовки, списки и блоки кода для наглядности.

5. КОНТЕКСТ:
- Проект: MTP_lab13
- Технологический стек: Go, Python (FastAPI, nats-py), NATS, Docker.
```

**Результат:** Создан файл `architecture.md` с подробным описанием архитектуры системы, диаграммой последовательности Mermaid и описанием всех компонентов и паттернов.

**Верификация**:
- Файл `architecture.md` создан и содержит все требуемые разделы.
- Проверено, что `README.md` не был изменен.

### Итого
- **Количество промптов:** 1
- **Что пришлось исправлять вручную:** Не требовалось.
- **Время:** ~10 мин
